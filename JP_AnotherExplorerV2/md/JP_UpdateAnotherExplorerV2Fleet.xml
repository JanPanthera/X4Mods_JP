<?xml version="1.0" encoding="utf-8" ?>

<mdscript name="JP_UpdateAnotherExplorerV2Fleet_MD" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="md.xsd">
  <cues>

    <cue name="JP_UPDATE_ANOTHER_EXPLORER_V2_FLEET" instantiate="true" checkinterval="2s">
      <conditions>
        <check_value value="global.$AnotherExplorerV2Ships? and @global.$AnotherExplorerV2Ships.count gt 1"/>
      </conditions>
      <actions>

        <set_value name="$DEBUG" exact="0"/>
        <do_if value="$DEBUG gt 0">
          <set_value name="$DebugDirectory" exact="'JP_AnotherExplorerV2.logs'"/>
          <set_value name="$DebugFileName" exact="'JP_UPDATE_ANOTHER_EXPLORER_V2_FLEET.xml.log'"/>
        </do_if>

        <do_if value="$DEBUG gt 0">
          <debug_to_file append="false" directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'[' + player.systemtime.{'%F - %X'} + '] ' + '~~ JP_UPDATE_ANOTHER_EXPLORER_V2_FLEET.xml ~ Started ~~' + '\n'"/>
        </do_if>

        <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

        <do_for_each name="$_AutoFleetShip" in="global.$AnotherExplorerV2Ships">
          <do_if value="$_AutoFleetShip.defaultorder.$IS_AUTO_FLEET_LEADER and $_AutoFleetShip.defaultorder.$AUTO_FLEET">
            <set_value name="$_AutoFleetShipLeader" exact="$_AutoFleetShip"/>
            <continue/>
          </do_if>
        </do_for_each>

        <do_if value="$_AutoFleetShipLeader?">
          <do_for_each name="$_AutoFleetShip" in="global.$AnotherExplorerV2Ships" counter="$_it">
            <do_if value="not $_AutoFleetShip.defaultorder.$AUTO_FLEET">
              <continue/>
            </do_if>
            <do_if value="$_AutoFleetShip != $_AutoFleetShipLeader and $_AutoFleetShip.commander == null">
              <set_object_commander object="$_AutoFleetShip" commander="$_AutoFleetShipLeader"/>
              <continue/>
            </do_if>
            <do_if value="$_AutoFleetShip == $_AutoFleetShipLeader and $_AutoFleetShip.fleet.name != ''">
              <set_value name="$_ColorString" exact="if @global.$AEV2_AFCS then global.$AEV2_AFCS else ''"/>
              <do_if value="$_AutoFleetShip.fleet.name != (if $_ColorString == '' then 'AnotherExplorerV2' else $_ColorString + 'AnotherExplorerV2' + '\033X')">
                <set_object_fleet_name object="$_AutoFleetShip" name="if $_ColorString != '' then $_ColorString + 'AnotherExplorerV2' + '\033X' else 'AnotherExplorerV2'"/>
              </do_if>
              <remove_value name="$_ColorString"/>
              <continue/>
            </do_if>
          </do_for_each>
        </do_if>

        <do_if value="$_AutoFleetShipLeader?">
          <remove_value name="$_AutoFleetShipLeader"/>
        </do_if>

        <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

        <do_if value="$DEBUG gt 0">
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'[' + player.systemtime.{'%F - %X'} + '] ' + '~~ JP_UPDATE_ANOTHER_EXPLORER_V2_FLEET.xml ~ Finished ~~'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~\n'"/>
        </do_if>

        <do_if value="$DEBUG gt 0">
          <remove_value name="$DebugFileName"/>
          <remove_value name="$DebugDirectory"/>
        </do_if>
        <remove_value name="$DEBUG"/>

      </actions>
    </cue>

    <cue name="JP_ANOTHER_EXPLORER_V2_DEBUG" instantiate="true" checkinterval="1s">
      <actions>

        <set_value name="$DEBUG" exact="0"/>
        <do_if value="$DEBUG gt 0">
          <set_value name="$DebugDirectory" exact="'JP_AnotherExplorerV2.logs'"/>
          <set_value name="$DebugFileName" exact="'JP_ANOTHER_EXPLORER_V2_DEBUG.xml.log'"/>
        </do_if>

        <do_if value="$DEBUG gt 0">
          <debug_to_file append="false" directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'[' + player.systemtime.{'%F - %X'} + '] ' + '~~ JP_ANOTHER_EXPLORER_V2_DEBUG.xml ~ Started ~~' + '\n'"/>
        </do_if>

        <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

        <do_if value="$DEBUG gt 0">
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'global.$AnotherExplorerV2SectorBlackList'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'"/>
          <do_if value="global.$AnotherExplorerV2SectorBlackList?">
            <do_for_each name="$_Sector" in="@global.$AnotherExplorerV2SectorBlackList">
              <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~ Sector: %1 ~~ [%2].'.[@$_Sector.knownname, @$_Sector.owner.knownname]"/>
            </do_for_each>
          </do_if>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~\n'"/>

          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'global.$AnotherExplorerV2Ships'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~'"/>
          <do_if value="global.$AnotherExplorerV2Ships?">
            <do_for_each name="$_Ship" in="@global.$AnotherExplorerV2Ships">
              <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~ Ship: %1 [%2] ~~ [%3].'.[@$_Ship.knownname, @$_Ship.idcode, @$_Ship.owner.knownname]"/>
            </do_for_each>
          </do_if>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~\n'"/>
        </do_if>

        <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

        <do_if value="$DEBUG gt 0">
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'[' + player.systemtime.{'%F - %X'} + '] ' + '~~ JP_ANOTHER_EXPLORER_V2_DEBUG.xml ~ Finished ~~'"/>
          <debug_to_file directory="$DebugDirectory" name="$DebugFileName" text="'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~\n'"/>
        </do_if>

        <do_if value="$DEBUG gt 0">
          <remove_value name="$DebugFileName"/>
          <remove_value name="$DebugDirectory"/>
        </do_if>
        <remove_value name="$DEBUG"/>

      </actions>
    </cue>

  </cues>
</mdscript>